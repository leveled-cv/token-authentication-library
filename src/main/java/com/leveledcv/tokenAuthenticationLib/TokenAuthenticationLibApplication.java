package com.leveledcv.tokenAuthenticationLib;

/**
 * Main class for the authentication library
 * This is a library package, not a standalone application
 */
public class TokenAuthenticationLibApplication {
    // This class serves as the main entry point for the library
    // It doesn't need a main method since it's intended to be used as a dependency
}
