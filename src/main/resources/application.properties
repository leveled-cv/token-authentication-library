spring.application.name=token-authentication-lib

# Authentication Library Configuration
# Configure the token verification service URL (can be changed per microservice)
auth.token-verify-url=http://localhost:8081/token/verify
auth.token-header=Authorization
auth.token-prefix=Bearer
